# Core dependencies for AI Fitness Coach
fastapi>=0.100.0
uvicorn[standard]>=0.25.0
pydantic>=2.5.0
python-multipart>=0.0.10
pydantic-settings>=2.0.0

# Lightweight RAG dependencies (faster build)
numpy>=1.24.0
scikit-learn>=1.3.0
rank-bm25>=0.2.2

# Optional ML dependencies (used as fallback if available)
# Install these if you want sentence-transformers support:
# transformers>=4.40.0
# sentence-transformers==2.7.0
# torch>=2.0.0
# accelerate>=0.20.0

# HTTP client for OpenRouter
httpx>=0.27.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
