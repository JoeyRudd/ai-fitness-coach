# Core dependencies for AI Fitness Coach
fastapi>=0.100.0
uvicorn[standard]>=0.25.0
pydantic>=2.5.0
python-multipart>=0.0.10
pydantic-settings>=2.0.0
google-generativeai>=0.7.0

# Lightweight RAG dependencies (faster build)
numpy>=1.24.0
scikit-learn>=1.3.0
rank-bm25>=0.2.2

# Heavy ML dependencies (optional, used as fallback)
# Uncomment if needed: transformers>=4.40.0
# Uncomment if needed: sentence-transformers==2.7.0
# Uncomment if needed: torch>=2.0.0
# Uncomment if needed: accelerate>=0.20.0

# Testing dependencies
httpx>=0.24.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
